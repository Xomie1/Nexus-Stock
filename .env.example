# Copy this to .env and fill in your values for local development
# On Render: add these in the dashboard under Environment Variables

# Optional — only needed if you re-enable Supabase integration
# SUPABASE_URL=https://your-project-id.supabase.co
# SUPABASE_KEY=your-anon-public-key

# PORT is set automatically by Render — do not override
